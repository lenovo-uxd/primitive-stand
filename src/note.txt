调整对比度饱和度

1. 人脸检测位置矩形
2. 矩形框内画圆或椭圆
3. 从r*4/5处开始模糊，透明度从1到0

做法：
获取人脸框内的所有像素值矩阵M，r为矩形短边的一半，计算每个点M[x,y]离矩形中心点O的距离d，每个点的透明度a={
    0, d > r;
    1, d < 0.8*r;
    5*(r-d)/r, 0.8*r < d < r;
}

1. 歡迎頁動圖
2. 感謝頁按鈕切圖
3. 返回按鈕的用處
4. 設計師工具顔色篩選和風格篩選的api字段


1. 按键弹起
2. 改键盘颜色
3. 键盘输入法
新旧同时动画，新透明度增加，旧减少，新的初始大小与旧相同，新旧按钮大小相同

1. 首页按下后保持按下状态，进行放大，在放大完成后，按钮突变
2. decr分为两张，男在下